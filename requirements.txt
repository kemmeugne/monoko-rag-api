# MONOKO - Lingala AI Agent Dependencies
# =======================================
# Install all: pip install -r requirements.txt

# Step 1: Data Download
datasets>=2.16.0
opustools>=1.5.0
requests>=2.31.0
tqdm>=4.66.0
pandas>=2.0.0
openpyxl>=3.1.0

# Step 2: Data Processing
# (uses only standard library + pandas from above)

# Step 3: RAG System
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
anthropic>=0.18.0
# openai>=1.12.0          # Uncomment if using OpenAI instead of Anthropic

# RAG API Server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
langdetect>=1.0.9

# Step 4: Voice Integration
elevenlabs>=1.0.0
sounddevice>=0.4.6
soundfile>=0.12.0
# openai-whisper>=20231117  # Uncomment for local Whisper (requires ffmpeg)
# edge-tts>=6.1.0          # Uncomment for free Microsoft TTS alternative
